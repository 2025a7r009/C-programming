#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int add(int *a,int*b){
    return *a+*b;
}

int main() {
    int a,b;
    scanf("%d %d",&a,&b);
    int c=add(&a,&b);
    printf("%d",c);
    return 0;
}
