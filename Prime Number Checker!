#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,count=2,f=1;
    scanf("%d",&n);
    while(count<n){
        if(n%count==0){
            f=0;
            break;
        }
        count++;
    }
    if(f==1)printf("Prime");
    else printf("Not Prime");
    return 0;
}
