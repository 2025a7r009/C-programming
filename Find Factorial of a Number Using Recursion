#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int fac(int n){
    if(n==0)return 1;
    else return n*fac(n-1);
}
int main() {
    int n;
    scanf("%d",&n);
    int f=fac(n);
    printf("%d",f);
    return 0;
}
