#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int RECURSION(int N){
    if (N==0) return 0;
    return (N%10)+RECURSION(N/10);
    
}

int main() {
    int N;
    scanf("%d",&N);
    int Sum=RECURSION(N);
    printf("%d",Sum);
    return 0;
}
