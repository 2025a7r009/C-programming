#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {   
    int n;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int found=0;
    for(int i=0;i<n;i++){
        int j;
        int count=0;
        for(j=0;j<i;j++){
            if(arr[j]==arr[i]){
                break;
            }
        }
        if(j==i){
            for(int k=i;k<n;k++){
                if(arr[i]==arr[k]){
                    count++;
                }
            }
            if(count==1){
                printf("%d ",arr[i]);
                found=1;
            }
            }
        
        }if(found==0){
        printf("No unique elements");
    }
    
    return 0;
}
