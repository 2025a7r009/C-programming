#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    for(int i=0;i<n;i++){
           int min=0;
        for(int j=1;j<n;j++){
            if(arr[j]<arr[min]){
                min=j;
            }
        }
        
   printf("%d ",arr[min]);
    arr[min]=932374;
    }
    return 0;
}
